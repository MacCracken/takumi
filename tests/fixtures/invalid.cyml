# Deliberately-invalid recipe for the integration harness: parses fine but
# fails validation (non-https source scheme). Kept OUT of fixtures/recipes/
# so it doesn't perturb the list/order/build-over-dir assertions.

[package]
name = "bad-scheme"
version = "1.0.0"
description = "invalid: ftp source scheme"
license = "MIT"

[source]
url = "ftp://example.com/bad-1.0.0.tar.gz"
sha256 = "e1f5c1c2b3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0"
