# takumi — Cyrius port
# Ported from 2034 lines of Rust

# Stdlib auto-included via cyrius.cyml

fn main() {
    alloc_init();
    println("takumi ready");
    return 0;
}

var r = main();
syscall(SYS_EXIT, r);
